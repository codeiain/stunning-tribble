import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile2-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
